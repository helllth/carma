import { carmaWuppertalCollab } from './carma-wuppertal-collab';

describe('carmaWuppertalCollab', () => {
  it('should work', () => {
    expect(carmaWuppertalCollab()).toEqual('carma-wuppertal-collab');
  });
});
